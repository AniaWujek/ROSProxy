# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(IntProxy)

ADD_COMPONENT(FloatProxy)

ADD_COMPONENT(Float32MultiArrayProxy)

ADD_COMPONENT(Float32MultiArrayProxy2)

Add_COMPONENT(StringProxy)
